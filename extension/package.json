{
  "name": "onlyfans-conversational-analytics-agent",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "test": "node --test",
    "build": "node build.mjs",
    "audit": "node build.mjs --audit",
    "qualify:snapshot": "node --expose-gc qualification/snapshot-100k.mjs",
    "qualify:snapshot:ci": "node --expose-gc qualification/snapshot-100k.mjs --messages=10000"
  },
  "dependencies": {
    "local-authenticated-read-connector": "file:vendor/local-authenticated-read-connector-0.2.0-beta.6.tgz"
  },
  "devDependencies": {
    "esbuild": "0.25.6"
  },
  "description": "The Agent is an MV3 browser extension for OnlyFans Conversational Analytics. It captures conversation data available to the logged-in creator, persists unacknowledged ingestion locally, and communicates with Brain through the role-specific protocol-v2 Agent channel.",
  "main": "background.js",
  "directories": {
    "test": "tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
