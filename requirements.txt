fastapi==0.139.2
uvicorn==0.51.0
httpx==0.28.1
websockets==16.1.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
broadcaster==0.3.1
networkx==3.6.1
jinja2==3.1.6
cryptography==50.0.0
# Windows builds use the reproducible, source-built wheel documented in
# packaging/sqlcipher/README.md. Linux development stays on the public wheel;
# it is not shipped by the Windows package and does not qualify Tier B.
sqlcipher3==0.6.2+ofca.1 ; platform_system == "Windows"
sqlcipher3==0.6.2 ; platform_system != "Windows"
