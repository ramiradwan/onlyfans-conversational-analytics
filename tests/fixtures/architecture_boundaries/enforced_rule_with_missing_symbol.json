{
  "scenario": "enforced_rule_with_missing_symbol",
  "description": "Enforced rule referencing non-existent symbol in existing file",
  "expected_diagnostic": "not found",
  "mutation": {
    "op": "append",
    "target": "rules",
    "item": {
      "id": "rule-test-enforced-missing-symbol",
      "description": "Test rule with missing symbol",
      "source_modules": [
        "canonical-persistence"
      ],
      "target_modules": [
        "security-trust"
      ],
      "type": "forbidden",
      "enforcement": {
        "status": "enforced",
        "enforced_by": [
          "tests/test_architecture_runtime_policy.py::non_existent_test_symbol"
        ]
      }
    }
  }
}
