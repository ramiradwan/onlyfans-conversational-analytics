{
  "scenario": "enforced_rule_with_external_ref",
  "description": "Enforced rule referencing path escaping the repository",
  "expected_diagnostic": "escapes repository root",
  "mutation": {
    "op": "append",
    "target": "rules",
    "item": {
      "id": "rule-test-enforced-external-ref",
      "description": "Test rule with path escaping repo",
      "source_modules": [
        "canonical-persistence"
      ],
      "target_modules": [
        "security-trust"
      ],
      "type": "forbidden",
      "enforcement": {
        "status": "enforced",
        "enforced_by": [
          "../outside_repo_file.py::test_func"
        ]
      }
    }
  }
}
