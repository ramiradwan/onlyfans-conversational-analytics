# ADR 0009 browser-host implementation-spike evidence

> Machine-generated by `npm run spike` at 2026-07-18T17:57:00.877Z. Do not hand-edit observed results.

## Verdict

**PASS — the browser host passes the ADR 0009 implementation-spike gate on this machine.** Current stable Chrome and Edge passed every automated browser criterion at the exact production candidate origin and RP ID, so these results do not mandate the signed desktop-shell fallback. Cutover still requires manual confirmation with the real Windows Hello platform authenticator and installer/launcher/updater origin-invariance evidence; neither caveat permits weakening the cookie or authentication contract.

## Criterion table

| # | Automated criterion | Chrome | Edge | Observed detail / deferred boundary |
| ---: | --- | --- | --- | --- |
| 1 | Loopback resolution | PASS — fresh: bridge.localhost and other.bridge.localhost loaded without DNS/hosts configuration; observed peers ::1 and ::1, both loopback. Reused-profile check: reused: bridge.localhost and other.bridge.localhost loaded without DNS/hosts configuration; observed peers ::1 and ::1, both loopback. | PASS — fresh: bridge.localhost and other.bridge.localhost loaded without DNS/hosts configuration; observed peers ::1 and ::1, both loopback. Reused-profile check: reused: bridge.localhost and other.bridge.localhost loaded without DNS/hosts configuration; observed peers ::1 and ::1, both loopback. | Both a new throwaway persistent profile and the same profile reopened were exercised. |
| 2 | Secure context and service worker | PASS — fresh: window.isSecureContext=true; service-worker registration succeeded with scope http://bridge.localhost:17871/. Reused-profile check: reused: window.isSecureContext=true; service-worker registration succeeded with scope http://bridge.localhost:17871/. | PASS — fresh: window.isSecureContext=true; service-worker registration succeeded with scope http://bridge.localhost:17871/. Reused-profile check: reused: window.isSecureContext=true; service-worker registration succeeded with scope http://bridge.localhost:17871/. | Both a new throwaway persistent profile and the same profile reopened were exercised. |
| 3 | Exact session-cookie contract | PASS — fresh: CDP reported the accepted host-only cookie as Secure, HttpOnly, SameSite=Strict, Path=/; document.cookie could not read it and the same-origin protected request received it. Reused-profile check: reused: CDP reported the accepted host-only cookie as Secure, HttpOnly, SameSite=Strict, Path=/; document.cookie could not read it and the same-origin protected request received it. | PASS — fresh: CDP reported the accepted host-only cookie as Secure, HttpOnly, SameSite=Strict, Path=/; document.cookie could not read it and the same-origin protected request received it. Reused-profile check: reused: CDP reported the accepted host-only cookie as Secure, HttpOnly, SameSite=Strict, Path=/; document.cookie could not read it and the same-origin protected request received it. | Both a new throwaway persistent profile and the same profile reopened were exercised. |
| 4 | Cookie isolation | PASS — fresh: No __Host-bridge_session cookie was sent to localhost, 127.0.0.1, or other.localhost. Reused-profile check: reused: No __Host-bridge_session cookie was sent to localhost, 127.0.0.1, or other.localhost. | PASS — fresh: No __Host-bridge_session cookie was sent to localhost, 127.0.0.1, or other.localhost. Reused-profile check: reused: No __Host-bridge_session cookie was sent to localhost, 127.0.0.1, or other.localhost. | Both a new throwaway persistent profile and the same profile reopened were exercised. |
| 5 | Cross-origin/port rejection | PASS — fresh: The cookie was observed on bridge.localhost:17872 (cookies are not port-scoped), but 17872-to-17871 use was rejected with 403. Browser-forged Host/Cookie headers were forbidden, and a raw request with exact Host plus a manually attached cookie was still rejected for wrong Origin. Reused-profile check: reused: The cookie was observed on bridge.localhost:17872 (cookies are not port-scoped), but 17872-to-17871 use was rejected with 403. Browser-forged Host/Cookie headers were forbidden, and a raw request with exact Host plus a manually attached cookie was still rejected for wrong Origin. | PASS — fresh: The cookie was observed on bridge.localhost:17872 (cookies are not port-scoped), but 17872-to-17871 use was rejected with 403. Browser-forged Host/Cookie headers were forbidden, and a raw request with exact Host plus a manually attached cookie was still rejected for wrong Origin. Reused-profile check: reused: The cookie was observed on bridge.localhost:17872 (cookies are not port-scoped), but 17872-to-17871 use was rejected with 403. Browser-forged Host/Cookie headers were forbidden, and a raw request with exact Host plus a manually attached cookie was still rejected for wrong Origin. | Cookies were explicitly observed as host-scoped but not port-scoped; exact Origin/Host validation is the compensating control within the accepted local-OS-compromise posture. |
| 6 | WebAuthn virtual platform authenticator | PASS — fresh: CTAP2 internal virtual-authenticator create/get succeeded for bridge.localhost with UV flags set; localhost failed (NotAllowedError), wrong.localhost failed (NotAllowedError), and required UV failed when isUserVerified=false (NotAllowedError). Reused-profile check: reused: CTAP2 internal virtual-authenticator create/get succeeded for bridge.localhost with UV flags set; localhost failed (NotAllowedError), wrong.localhost failed (NotAllowedError), and required UV failed when isUserVerified=false (NotAllowedError). | PASS — fresh: CTAP2 internal virtual-authenticator create/get succeeded for bridge.localhost with UV flags set; localhost failed (NotAllowedError), wrong.localhost failed (NotAllowedError), and required UV failed when isUserVerified=false (NotAllowedError). Reused-profile check: reused: CTAP2 internal virtual-authenticator create/get succeeded for bridge.localhost with UV flags set; localhost failed (NotAllowedError), wrong.localhost failed (NotAllowedError), and required UV failed when isUserVerified=false (NotAllowedError). | The CDP authenticator simulates a platform authenticator; real Windows Hello confirmation is deferred to cutover E2E. |
| 7 | Port conflict, refusal, release, and reacquire | PASS — shared harness check | PASS — shared harness check | Conflict was detected with actionable remediation; the unrelated owner remained alive. After release, the harness acquired 17871 and reacquired it on restart. Installer/launcher/updater lifecycle origin invariance is **DEFERRED** to the installer phase because those components do not exist in this isolated browser spike. |
| 8 | Both stable browser channels and profile states | PASS — Chrome 150.0.7871.125, fresh + reused profiles | PASS — Edge 151.0.4129.21, fresh + reused profiles | All browser checks ran headless unless an individual WebAuthn result explicitly records a headed fallback. |

## Environment and exact versions

- OS: Windows_NT 10.0.26300; Windows 11 Pro; x64
- Node: v22.18.0
- Playwright: 1.61.1
- Chrome channel `chrome`: Chrome/150.0.7871.125 (Playwright-reported version 150.0.7871.125)
- Edge channel `msedge`: Edg/151.0.4129.21 (Playwright-reported version 151.0.4129.21)
- Candidate origin: `http://bridge.localhost:17871`
- WebAuthn RP ID: `bridge.localhost`
- Listener bindings: `127.0.0.1:17871`, `::1:17871`, `127.0.0.1:17872`, `::1:17872`
- Browser source: system-installed stable channels only. The runner passed Playwright channel names and did not request a bundled executable.

## Automation boundary and caveats

- The harness set exactly `__Host-bridge_session=adr-0009-spike-session; Secure; HttpOnly; SameSite=Strict; Path=/`. CDP `Network.getCookies`, rather than `document.cookie`, supplied the acceptance and attribute evidence. The raw observations are in `results.json`; every received Host/Origin/Cookie tuple is in `request-log.json`.
- The CDP CTAP2/internal virtual authenticator had `hasUserVerification=true` and `isUserVerified=true`. It exercises the browser's WebAuthn path and response flags but only simulates Windows Hello. A manual enrollment/assertion with the real platform authenticator remains required for cutover E2E.
- Cookies are not port-scoped. The observed cookie delivery to `bridge.localhost:17872` is an accepted local-OS-compromise residual risk, mitigated at Brain by exact Host and Origin checks; it is not evidence of transport encryption.
- No hosts-file entry, DNS configuration, browser policy, or certificate was created. The browser connections observed by the server were loopback connections. This does not claim that application-level JavaScript can reveal Chromium's internal resolver path.
- Installer/launcher/updater origin invariance is deferred to the installer phase. The isolated spike can prove fixed-port conflict/refusal/reacquisition, but it cannot test components that do not yet exist.
- Playwright is a devDependency only of this spike directory. `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` was used for installation; no bundled browser was downloaded or launched.
