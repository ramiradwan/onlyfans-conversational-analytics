# Vendored contract fixture bytes are part of their SHA-256 integrity boundary.
contracts/** text eol=lf

# Compact cryptographic encodings are opaque bytes, not text to normalize.
contracts/**/*.jws -text !eol
contracts/**/*.jwk -text !eol

# Migration checksums are calculated from raw catalog bytes.
app/persistence/sql/** text eol=lf
app/persistence/auth_sql/** text eol=lf
app/persistence/projection_sql/** text eol=lf
app/analytics/sql/** text eol=lf

# The creator disclosure is served and integrity-tested as frozen UTF-8 bytes.
app/provisioning/creator-platform-data-risk-disclosure.html text eol=lf

# Cross-repository attestation vectors are compared as exact published text.
tests/fixtures/engineering-attestation-v1-ed25519.json text eol=lf

# Vendored Legal evidence JSON is integrity-checked as exact source bytes.
shared/legal/*.json text eol=lf

# Native Snow qualification lock is a reviewed supply-chain input and must be
# byte-identical across Windows and non-Windows checkouts.
native/companion-snow/** text eol=lf
extension/crypto/snow/** text eol=lf
extension/vendor/companion-snow/*.js text eol=lf
extension/vendor/companion-snow/*.json text eol=lf
extension/vendor/companion-snow/*.txt text eol=lf
extension/vendor/companion-snow/*.wasm binary

# A Legal instrument bindings document is stored in its canonical form and is
# digested as the bytes it is read as, so it is never rewritten on checkout.
extension/tests/fixtures/legal-instrument-bindings*.json -text

# Fixture cases are generated review noise; manifests, pins, schemas, docs, and
# loaders remain visible because they define the reviewable contract boundary.
contracts/grant-profile-v1/*/*/** linguist-generated=true

# Generated upstream license notices retain their original internal whitespace.
native/companion-snow/THIRD_PARTY_NOTICES.txt -whitespace
extension/vendor/companion-snow/THIRD_PARTY_NOTICES.txt -whitespace
